import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // next.config.js
images: {
  domains: ['localhost'], // or your deployed domain later
}

};

export default nextConfig;
